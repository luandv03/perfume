import { Flex, Stack, Text } from "@mantine/core";

export const OrderDetail = () => {
    return (
        <Stack>
            <Text fw={700}>Order</Text>
            <Flex></Flex>
        </Stack>
    );
};
